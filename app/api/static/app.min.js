const App={tg:null,initData:"",shopId:1,currentCategory:null,categories:[],selectedVariant:null,cartTotal:0,appliedPromo:null,paymentMethods:[],selectedPaymentMethod:"manual",productAttrs:[],attrLabels:{},company:null,botUsername:null,currentRating:0,async init(){this.tg=window.Telegram.WebApp;this.tg.ready();this.tg.expand();this.initData=this.tg.initData||"";const params=new URLSearchParams(window.location.search);const startParam=this.tg.initDataUnsafe?.start_param||"";if(startParam.startsWith("shop_")){this.shopId=parseInt(startParam.replace("shop_",""));}else{this.shopId=parseInt(params.get("shop"))||1;}
await Promise.all([this.loadShopConfig(),this.loadCategories(),this.updateCartBadge(),this.loadOffers(),]);},async api(method,path,body){const opts={method,headers:{"Authorization":"tma "+this.initData,"X-Shop-Id":String(this.shopId),},};if(body){opts.headers["Content-Type"]="application/json";opts.body=JSON.stringify(body);}
const resp=await fetch("/api/shop"+path,opts);if(!resp.ok){const err=await resp.json().catch(()=>({}));const error=new Error(err.detail?.error||err.detail||"Ошибка запроса");if(typeof err.detail==="object"&&err.detail!==null){error.detail=err.detail;}
throw error;}
if(resp.status===204)return null;return resp.json();},photoUrl(photoId){if(!photoId)return null;return`/api/shop/photo/${photoId}`;},offers:{},async loadOffers(){try{const offers=await this.api("GET","/my-offers");this.offers={};if(offers&&offers.length){offers.forEach(o=>{const key=o.variant_id?`${o.product_id}_${o.variant_id}`:`${o.product_id}`;this.offers[key]=o;});}}catch(e){}},hasOffer(productId){return Object.keys(this.offers).some(k=>k.startsWith(`${productId}_`)||k===`${productId}`);},showView(viewId){document.querySelectorAll(".view").forEach(v=>v.classList.remove("active"));document.getElementById("view-"+viewId).classList.add("active");const showNav=!["product","checkout","success","privacy","out-of-stock"].includes(viewId);document.getElementById("bottom-nav").style.display=showNav?"flex":"none";window.scrollTo(0,0);document.querySelectorAll(".nav-btn").forEach(btn=>btn.classList.remove("active"));},async loadShopConfig(){try{const cfg=await this.api("GET","/shop-config");if(cfg.product_attrs)this.productAttrs=cfg.product_attrs;if(cfg.attr_labels)this.attrLabels=cfg.attr_labels;if(cfg.company)this.company=cfg.company;if(cfg.bot_username)this.botUsername=cfg.bot_username;if(cfg.theme)this.applyTheme(cfg.theme);}catch(e){}},applyTheme(theme){const root=document.documentElement.style;const map={primary_color:"--button",bg_color:"--bg",text_color:"--text",button_text_color:"--button-text",secondary_bg_color:"--secondary-bg",radius:"--radius",};for(const[k,cssVar]of Object.entries(map)){if(theme[k])root.setProperty(cssVar,theme[k]);}
if(theme.font_family)document.body.style.fontFamily=theme.font_family;},async loadCategories(){try{this.categories=await this.api("GET","/categories");if(this.categories.length>0){this.selectCategory(this.categories[0]);}}catch(e){document.getElementById("products").innerHTML=`<div class="error-msg">Ошибка загрузки</div>`;}},selectCategory(cat){this.currentCategory=cat;this.renderCategoryChips();this.loadProducts(cat.id);},renderCategoryChips(){const container=document.getElementById("categories");container.innerHTML=this.categories.map(c=>`
            <button class="cat-chip ${c.id === this.currentCategory.id ? 'active' : ''}"
                    onclick="App.selectCategory(${JSON.stringify(c).replace(/"/g, '&quot;')})">
                ${c.emoji || ""} ${c.name}
            </button>
        `).join("");},async loadProducts(categoryId){const grid=document.getElementById("products");grid.innerHTML=`<div class="loading">Загрузка...</div>`;try{const products=await this.api("GET",`/products?category_id=${categoryId}`);if(products.length===0){grid.innerHTML=`<div class="loading">Нет товаров</div>`;return;}
grid.innerHTML=products.map(p=>{const photo=this.photoUrl(p.photo_id);const rating=p.rating?`⭐ ${p.rating.avg} (${p.rating.count})`:"";const offerBadge=p.has_offer?`<div class="offer-badge">🔥 Персональная скидка</div>`:"";return`
                    <div class="product-card" onclick="App.openProduct(${p.id})">
                        ${offerBadge}
                        ${photo
                            ? `<img src="${photo}"loading="lazy"onerror="this.style.display='none';this.nextElementSibling.style.display='flex'"><div class="placeholder"style="display:none">${this.catEmoji()}</div>`
                            : `<div class="placeholder">${this.catEmoji()}</div>`
                        }
                        <div class="info">
                            <div class="name">${this.esc(p.name)}</div>
                            <div class="price">от ${p.price_from} ₽</div>
                            ${rating ? `<div class="rating">${rating}</div>` : ""}
                        </div>
                    </div>
                `;}).join("");}catch(e){grid.innerHTML=`<div class="error-msg">Ошибка загрузки</div>`;}},catEmoji(){return this.currentCategory?.emoji||"📦";},async openProduct(productId){this.showView("product");const detail=document.getElementById("product-detail");detail.innerHTML=`<div class="loading">Загрузка...</div>`;try{const p=await this.api("GET",`/products/${productId}`);this.selectedVariant=p.variants[0];detail.innerHTML=this.renderProductDetail(p);}catch(e){detail.innerHTML=`<div class="error-msg">Ошибка: ${this.esc(e.message)}</div>`;}},renderProductDetail(p){const photos=(p.photos||[]).map(ph=>this.photoUrl(ph.id)).filter(Boolean);const rating=p.rating?`⭐ ${p.rating.avg} (${p.rating.count} отз.)`:"Нет отзывов";let html="";if(photos.length>0){const slides=photos.map((src,i)=>`<div class="gallery-slide"><img src="${src}" onerror="this.parentElement.style.display='none'"></div>`).join("");const dots=photos.map((_,i)=>`<span class="gallery-dot${i === 0 ? ' active' : ''}" onclick="App.scrollToSlide(${i})"></span>`).join("");html+=`
                <div class="gallery" id="product-gallery">
                    <div class="gallery-track" onscroll="App.onGalleryScroll()">
                        ${slides}
                    </div>
                    ${photos.length > 1 ? `<div class="gallery-dots"id="gallery-dots">${dots}</div>` : ''}
                </div>
            `;}else{html+=`<div class="gallery-placeholder">${this.catEmoji()}</div>`;}
html+=`<div class="pd-title">${this.esc(p.name)}</div>`;html+=`<div class="pd-rating">${rating}</div>`;html+=`<div class="pd-desc">${this.esc(p.description)}</div>`;const hasAttrs=this.productAttrs.length>0;if(hasAttrs){const labels=this.productAttrs.map(k=>this.attrLabels[k]||k).join(" / ");html+=`<div class="pd-label">${this.esc(labels)}</div>`;}
html+=`<div class="variants">`;p.variants.forEach((v,i)=>{const active=i===0?"active":"";const outOfStock=v.stock!==undefined&&v.stock===0;const stockHint=v.stock!==undefined&&v.stock>0&&v.stock<=5?`<span class="v-stock-low">осталось ${v.stock} шт</span>`:"";const stockBadge=outOfStock?`<span class="v-out-of-stock">нет в наличии</span>`:stockHint;let priceHtml;if(v.original_price&&v.discount_percent>0){const timerHtml=v.offer_expires_at?`<span class="v-timer" id="offer-timer-${v.id}"></span>`:"";priceHtml=`<span class="v-price"><span class="price-old">${v.original_price} ₽</span> <span class="price-new">${v.price} ₽</span> ${timerHtml}</span>`;}else{priceHtml=`<span class="v-price">${v.price} ₽</span>`;}
const attrParts=this.productAttrs.map(k=>(v.attributes&&v.attributes[k])||v[k]).filter(val=>val);const attrLabel=attrParts.length>0?attrParts.join(" · "):"—";html+=`
                <button class="variant-btn ${active} ${outOfStock ? 'variant-oos' : ''}" data-vid="${v.id}"
                        onclick="App.selectVariant(${v.id})">
                    ${this.esc(attrLabel)}
                    ${priceHtml}
                    ${stockBadge}
                </button>
            `;});const firstWithExpiry=p.variants.find(v=>v.offer_expires_at);if(firstWithExpiry){this.startOfferTimer(firstWithExpiry.offer_expires_at,`offer-timer-${firstWithExpiry.id}`);}
html+=`</div>`;const selected=p.variants[0]||{};const allOutOfStock=p.variants.every(v=>v.stock!==undefined&&v.stock===0);if(allOutOfStock){html+=`<button class="btn-primary btn-add" disabled>Нет в наличии</button>`;}else{html+=`<button class="btn-primary btn-add" onclick="App.addToCart(${p.id})">🛒 Добавить в корзину</button>`;}
if(this.botUsername){html+=`<div class="pd-actions">`;html+=`<button class="btn-primary" onclick="App.toggleReviewForm()">⭐ Оставить отзыв</button>`;html+=`<button class="btn-primary" onclick="App.toggleManagerForm()">💬 Написать менеджеру</button>`;html+=`</div>`;html+=`
                <div id="review-form-wrap" class="pd-form-wrap" style="display:none;">
                    <h3>Оставить отзыв</h3>
                    <div class="stars-input" id="stars-input">
                        ${[1, 2, 3, 4, 5].map(v => `<span class="star"data-val="${v}"onclick="App.setRating(${v})">★</span>`).join("")}
                    </div>
                    <textarea id="review-text" rows="3" maxlength="500" placeholder="Расскажите о своих впечатлениях..."></textarea>
                    <button class="btn-primary" onclick="App.submitReview(${p.id})">Отправить отзыв</button>
                    <button class="btn-link" onclick="App.toggleReviewForm()">Отмена</button>
                </div>
            `;html+=`
                <div id="manager-form-wrap" class="pd-form-wrap" style="display:none;">
                    <h3>Написать менеджеру</h3>
                    <textarea id="manager-text" rows="3" maxlength="500" placeholder="Ваш вопрос..."></textarea>
                    <button class="btn-primary" onclick="App.submitManagerMessage(${p.id})">Отправить</button>
                    <button class="btn-link" onclick="App.toggleManagerForm()">Отмена</button>
                </div>
            `;}
if(p.reviews&&p.reviews.length>0){html+=`<div class="reviews-section">`;html+=`<h3>Отзывы</h3>`;p.reviews.forEach(r=>{html+=`
                    <div class="review-item">
                        <div class="stars">${"⭐".repeat(r.rating)}</div>
                        ${r.text ? `<div class="text">${this.esc(r.text)}</div>` : ""}
                    </div>
                `;});html+=`</div>`;}
return html;},selectVariant(variantId){const btn=document.querySelector(`.variant-btn[data-vid="${variantId}"]`);if(btn&&btn.classList.contains("variant-oos")){this.toast("Этот вариант нет в наличии");return;}
this.selectedVariant={id:variantId};document.querySelectorAll(".variant-btn").forEach(b=>{b.classList.toggle("active",parseInt(b.dataset.vid)===variantId);});this.tg.HapticFeedback?.selectionChanged();},onGalleryScroll(){const track=document.getElementById("product-gallery")?.querySelector(".gallery-track");if(!track)return;const idx=Math.round(track.scrollLeft/track.clientWidth);document.querySelectorAll("#gallery-dots .gallery-dot").forEach((d,i)=>{d.classList.toggle("active",i===idx);});},scrollToSlide(idx){const track=document.getElementById("product-gallery")?.querySelector(".gallery-track");if(!track)return;track.scrollTo({left:idx*track.clientWidth,behavior:"smooth"});},toggleReviewForm(){const wrap=document.getElementById("review-form-wrap");const mgrWrap=document.getElementById("manager-form-wrap");if(mgrWrap)mgrWrap.style.display="none";const isOpen=wrap.style.display==="block";wrap.style.display=isOpen?"none":"block";if(!isOpen){this.currentRating=0;this.updateStars();const ta=document.getElementById("review-text");if(ta)ta.value="";wrap.scrollIntoView({behavior:"smooth",block:"center"});}},toggleManagerForm(){const wrap=document.getElementById("manager-form-wrap");const revWrap=document.getElementById("review-form-wrap");if(revWrap)revWrap.style.display="none";const isOpen=wrap.style.display==="block";wrap.style.display=isOpen?"none":"block";if(!isOpen){const ta=document.getElementById("manager-text");if(ta)ta.value="";wrap.scrollIntoView({behavior:"smooth",block:"center"});}},setRating(val){this.currentRating=val;this.updateStars();this.tg.HapticFeedback?.selectionChanged();},updateStars(){document.querySelectorAll("#stars-input .star").forEach(s=>{const v=parseInt(s.dataset.val);s.classList.toggle("filled",v<=this.currentRating);});},async submitReview(productId){if(!this.currentRating){this.toast("Выберите оценку");return;}
const text=(document.getElementById("review-text").value||"").trim()||null;try{await this.api("POST","/reviews",{product_id:productId,rating:this.currentRating,text,});this.tg.HapticFeedback?.notificationOccurred("success");this.toast("Спасибо за отзыв!");await this.openProduct(productId);}catch(e){this.toast(e.message);}},async submitManagerMessage(productId){const message=(document.getElementById("manager-text").value||"").trim();if(message.length<3){this.toast("Введите сообщение");return;}
try{await this.api("POST","/contact-manager",{product_id:productId,message,});this.tg.HapticFeedback?.notificationOccurred("success");this.toast("Сообщение отправлено менеджеру");this.toggleManagerForm();}catch(e){this.toast(e.message);}},async addToCart(productId){const variantId=this.selectedVariant?.id;if(!variantId){this.toast("Выберите объём");return;}
try{await this.api("POST","/cart/add",{product_id:productId,variant_id:variantId,});this.toast("Добавлено в корзину ✅");this.tg.HapticFeedback?.notificationOccurred("success");this.updateCartBadge();}catch(e){this.toast(e.message);}},showCatalog(){this.showView("catalog");document.querySelector('.nav-btn')?.classList.add("active");},async showCart(){this.showView("cart");document.querySelectorAll(".nav-btn")[1]?.classList.add("active");const container=document.getElementById("cart-items");const footer=document.getElementById("cart-footer");container.innerHTML=`<div class="loading">Загрузка...</div>`;footer.innerHTML="";try{const cart=await this.api("GET","/cart");if(cart.items.length===0){container.innerHTML=`
                    <div class="cart-empty">
                        <div class="emoji">🛒</div>
                        <div>Корзина пуста</div>
                    </div>
                `;return;}
container.innerHTML=cart.items.map(item=>{let priceHtml;if(item.original_price&&item.discount_percent>0){priceHtml=`<span class="price-old">${item.original_price} ₽</span> <span class="price-new">${item.price} ₽</span> · ${item.subtotal} ₽`;}else{priceHtml=`${item.subtotal} ₽`;}
const stock=item.stock;const atMax=stock!==undefined&&stock!==null&&item.quantity>=stock;const overStock=stock!==undefined&&stock!==null&&item.quantity>stock;const stockHint=overStock?`<div class="ci-stock-warn">В наличии только ${stock} шт.</div>`:(stock!==undefined&&stock!==null&&stock<=5&&stock>0?`<div class="ci-stock-low">Осталось ${stock} шт.</div>`:"");return`
                <div class="cart-item">
                    <div class="ci-info">
                        <div class="ci-name">${this.esc(item.product_name)}</div>
                        <div class="ci-variant">${this.esc(item.volume)}</div>
                        <div class="ci-price">${priceHtml}</div>
                        ${stockHint}
                    </div>
                    <div class="ci-controls">
                        <button class="qty-btn" onclick="App.changeQty(${item.cart_item_id}, -1)">−</button>
                        <span class="qty-display">${item.quantity}</span>
                        <button class="qty-btn${atMax ? ' qty-btn-disabled' : ''}" ${atMax ? 'disabled' : ''} onclick="App.changeQty(${item.cart_item_id}, 1)">+</button>
                    </div>
                </div>
            `}).join("");this.cartTotal=cart.total;footer.innerHTML=`
                <div class="cart-total">Итого: ${cart.total} ₽</div>
                <button class="btn-primary" onclick="App.showCheckout()">Оформить заказ</button>
            `;}catch(e){container.innerHTML=`<div class="error-msg">Ошибка: ${this.esc(e.message)}</div>`;}},async changeQty(itemId,delta){const path=delta>0?"cart/inc":"cart/dec";try{await this.api("POST",`/${path}/${itemId}`);await this.showCart();this.updateCartBadge();}catch(e){this.toast(e.message);}},async updateCartBadge(){try{const cart=await this.api("GET","/cart");const count=cart.items.length;const badge=document.getElementById("cart-badge");if(count>0){badge.textContent=count;badge.style.display="flex";}else{badge.style.display="none";}}catch(e){}},showCheckout(){if(this.cartTotal===0){this.toast("Корзина пуста");return;}
this.showView("checkout");this.appliedPromo=null;document.getElementById("co-promo").value="";document.getElementById("promo-result").textContent="";document.getElementById("promo-result").className="";this.renderCheckoutTotal();this.loadPaymentMethods();},async loadPaymentMethods(){const container=document.getElementById("payment-methods");container.innerHTML=`<div class="loading">Загрузка...</div>`;try{this.paymentMethods=await this.api("GET","/payment-methods");if(this.paymentMethods.length===0){container.innerHTML="";return;}
this.selectedPaymentMethod=this.paymentMethods[0].id;container.innerHTML=this.paymentMethods.map((m,i)=>`
                <label class="payment-option ${i === 0 ? 'active' : ''}" data-pm="${m.id}">
                    <input type="radio" name="payment_method" value="${m.id}" ${i === 0 ? 'checked' : ''}
                           onchange="App.selectPaymentMethod('${m.id}')">
                    <div class="pm-info">
                        <div class="pm-label">${this.esc(m.label)}</div>
                        <div class="pm-desc">${this.esc(m.description)}</div>
                    </div>
                </label>
            `).join("");}catch(e){container.innerHTML=`<div class="error-msg">Ошибка загрузки способов оплаты</div>`;}},selectPaymentMethod(methodId){this.selectedPaymentMethod=methodId;document.querySelectorAll(".payment-option").forEach(el=>{el.classList.toggle("active",el.dataset.pm===methodId);});this.tg.HapticFeedback?.selectionChanged();},async applyPromo(){const code=document.getElementById("co-promo").value.trim();const result=document.getElementById("promo-result");if(!code){result.textContent="Введите промокод";result.className="error";return;}
result.textContent="Проверка...";try{const promo=await this.api("POST","/promo/validate",{code});this.appliedPromo=promo;result.textContent=`✅ Скидка: ${promo.discount_amount} ₽`;result.className="success";this.tg.HapticFeedback?.notificationOccurred("success");this.renderCheckoutTotal();}catch(e){this.appliedPromo=null;result.textContent="❌ "+e.message;result.className="error";this.renderCheckoutTotal();}},renderCheckoutTotal(){const el=document.getElementById("co-total");if(this.appliedPromo){el.innerHTML=`
                <div class="subtotal-line">Без скидки: ${this.cartTotal} ₽</div>
                <div class="discount-line">Скидка: −${this.appliedPromo.discount_amount} ₽</div>
                <div>Итого: ${this.appliedPromo.final_total} ₽</div>
            `;}else{el.textContent=`Итого: ${this.cartTotal} ₽`;}},async submitOrder(event){event.preventDefault();const name=document.getElementById("co-name").value.trim();const phone=document.getElementById("co-phone").value.trim();const comment=document.getElementById("co-comment").value.trim()||null;if(name.length<2){this.toast("Введите имя");return;}
if(!document.getElementById("co-consent").checked){this.toast("Подтвердите согласие на обработку данных");return;}
try{const result=await this.api("POST","/orders",{full_name:name,phone,comment,promo_code:this.appliedPromo?.code||null,payment_method:this.selectedPaymentMethod,});this.updateCartBadge();this.showSuccess(result);this.tg.HapticFeedback?.notificationOccurred("success");}catch(e){if(e.detail&&e.detail.error==="out_of_stock"){this.showOutOfStock(e.detail.items);}else{this.toast(e.message);}}},showPrivacy(){this.showView("privacy");document.getElementById("privacy-content").innerHTML=this.privacyPolicyText();window.scrollTo(0,0);},privacyPolicyText(){const c=this.company;const contacts=c&&(c.name||c.inn||c.address)?`
            <h3>7. Контакты</h3>
            ${c.name ? `<p><b>${this.esc(c.name)}</b></p>` : ""}
            ${c.inn ? `<p>ИНН:${this.esc(c.inn)}</p>` : ""}
            ${c.address ? `<p>Адрес:${this.esc(c.address)}</p>` : ""}
            <p>По вопросам обработки персональных данных свяжитесь через чат в Telegram.</p>
        `:`
            <h3>7. Контакты</h3>
            <p>По вопросам обработки персональных данных свяжитесь с администратором магазина через чат в Telegram.</p>
        `;return`
            <h3>1. Общие положения</h3>
            <p>Настоящая Политика конфиденциальности определяет порядок обработки и защиты персональных данных пользователей магазина в Telegram.</p>
            <p>Используя магазин, вы автоматически соглашаетесь с настоящей Политикой.</p>

            <h3>2. Какие данные мы собираем</h3>
            <ul>
                <li>Имя и фамилия (указываются при оформлении заказа)</li>
                <li>Номер телефона (для связи по заказу)</li>
                <li>Telegram ID и username (получаются автоматически от Telegram)</li>
                <li>Состав и история заказов</li>
            </ul>

            <h3>3. Зачем мы используем данные</h3>
            <ul>
                <li>Обработка и доставка заказов</li>
                <li>Связь с покупателем по вопросам заказа</li>
                <li>Уведомления о статусе заказа</li>
            </ul>

            <h3>4. Хранение и защита</h3>
            <p>Данные хранятся на защищённом сервере. Мы не передаём ваши персональные данные третьим лицам, за исключением случаев, предусмотренных законом, и служб доставки.</p>

            <h3>5. Платёжные данные</h3>
            <p>При оплате онлайн ваши платёжные данные обрабатываются платёжным провайдером (ЮKassa). Мы не храним данные вашей банковской карты.</p>

            <h3>6. Удаление данных</h3>
            <p>Вы можете запросить удаление ваших персональных данных, написав сообщение боту или связавшись с администратором магазина.</p>

            ${contacts}
        `;},showSuccess(result){this.showView("success");const content=document.getElementById("success-content");let paymentInfo="";if(result.payment==="yookassa"&&result.confirmation_url){paymentInfo=`
                <button class="btn-primary" onclick="window.open('${result.confirmation_url}', '_blank')">
                    💳 Оплатить ${result.total} ₽
                </button>
                <div class="info">После оплаты статус заказа обновится автоматически.</div>
            `;}else if(result.payment==="manual"){if(result.payment_error){paymentInfo=`
                    <div class="info">⚠️ Онлайн-оплата временно недоступна.</div>
                    <div class="card">
                        <div style="font-weight:600;margin-bottom:8px">💳 Оплата переводом на карту:</div>
                        <div style="font-size:18px;font-weight:700;letter-spacing:1px">${this.esc(result.card_number || "не указан")}</div>
                        ${result.recipient ? `<div style="margin-top:4px;color:var(--hint)">Получатель:${this.esc(result.recipient)}</div>` : ""}
                    </div>
                `;}else{paymentInfo=`
                    <div class="card">
                        <div style="font-weight:600;margin-bottom:8px">💳 Оплата переводом на карту:</div>
                        <div style="font-size:18px;font-weight:700;letter-spacing:1px">${this.esc(result.card_number || "не указан")}</div>
                        ${result.recipient ? `<div style="margin-top:4px;color:var(--hint)">Получатель:${this.esc(result.recipient)}</div>` : ""}
                    </div>
                    <div class="info">После оплаты отправьте фото чека боту в чат.</div>
                `;}}
content.innerHTML=`
            <div class="emoji">✅</div>
            <h2>Заказ №${result.order_id} оформлен!</h2>
            ${result.discount > 0
                ? `<div class="info">Скидка:<b>−${result.discount}₽</b></div>`
                : ""
            }
            <div class="info">Сумма: <b>${result.total} ₽</b></div>
            ${paymentInfo}
            <button class="btn-primary" onclick="App.showCatalog()">Продолжить покупки</button>
        `;},showOutOfStock(items){this.showView("out-of-stock");this.tg.HapticFeedback?.notificationOccurred("error");const content=document.getElementById("out-of-stock-content");const itemsList=items.map(i=>`<div class="oos-item">
                <span class="oos-name">${this.esc(i.product_name)}</span>
                ${i.volume ? `<span class="oos-volume">${this.esc(i.volume)}</span>` : ""}
                <span class="oos-avail">В наличии: ${i.available} шт.</span>
            </div>`).join("");const contactBtn=this.botUsername?`<button class="btn-primary" onclick="App.tg.openTelegramLink('https://t.me/${this.botUsername}')">💬 Связаться с менеджером</button>`:`<button class="btn-primary" onclick="App.tg.close()">💬 Написать менеджеру</button>`;content.innerHTML=`
            <div class="emoji">😔</div>
            <h2>К сожалению, товар закончился</h2>
            <div class="info">Пока вы оформляли заказ, кто-то успел купить товар раньше.</div>
            <div class="oos-list">${itemsList}</div>
            <button class="btn-primary" onclick="App.showCart()">🛒 Перейти в корзину</button>
            ${contactBtn}
            <button class="btn-secondary" onclick="App.showCatalog()">В каталог</button>
        `;},async showOrders(){this.showView("orders");document.querySelectorAll(".nav-btn")[2]?.classList.add("active");const list=document.getElementById("orders-list");list.innerHTML=`<div class="loading">Загрузка...</div>`;try{const orders=await this.api("GET","/orders");if(orders.length===0){list.innerHTML=`
                    <div class="cart-empty">
                        <div class="emoji">📦</div>
                        <div>Заказов пока нет</div>
                    </div>
                `;return;}
const statusLabels={"new":"🆕 Новый","confirmed":"✅ Подтверждён","paid":"💰 Оплачен","shipped":"🚚 Отправлен","done":"🏁 Выполнен","cancelled":"❌ Отменён",};list.innerHTML=orders.map(o=>`
                <div class="order-card">
                    <div class="oc-header">
                        <span class="oc-id">Заказ №${o.id}</span>
                        <span class="oc-status">${statusLabels[o.status] || o.status}</span>
                    </div>
                    <div class="oc-total">${o.total_amount} ₽</div>
                    <div class="oc-date">${new Date(o.created_at).toLocaleDateString("ru-RU")}</div>
                </div>
            `).join("");}catch(e){list.innerHTML=`<div class="error-msg">Ошибка: ${this.esc(e.message)}</div>`;}},_offerTimerInterval:null,startOfferTimer(isoStr,elementId){if(this._offerTimerInterval){clearInterval(this._offerTimerInterval);}
const target=new Date(isoStr).getTime();const update=()=>{const el=document.getElementById(elementId);if(!el){clearInterval(this._offerTimerInterval);return;}
const remaining=target-Date.now();if(remaining<=0){el.textContent="истекло";clearInterval(this._offerTimerInterval);return;}
const h=Math.floor(remaining/3600000);const m=Math.floor((remaining%3600000)/60000);const s=Math.floor((remaining%60000)/1000);el.textContent=`⏰ ${h}ч ${m}м ${s}с`;};update();this._offerTimerInterval=setInterval(update,1000);},toast(msg){if(this.tg.HapticFeedback){this.tg.HapticFeedback.notificationOccurred("error");}
this.tg.showAlert?this.tg.showAlert(msg):alert(msg);},esc(text){if(!text)return"";const div=document.createElement("div");div.textContent=text;return div.innerHTML;},};App.init();