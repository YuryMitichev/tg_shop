BOT_TOKEN=your_bot_token_here
DEBUG=False
JWT_SECRET=generate-with-python-secrets-token-hex-32
CORS_ORIGINS=http://localhost:3000
MANAGER_CHAT_ID=
ADMIN_IDS=
DATABASE_URL=sqlite+aiosqlite:///./shop.db

# В Docker (docker-compose.yml) DATABASE_URL переопределяется на PostgreSQL:
# postgresql+asyncpg://tg_shop:$POSTGRES_PASSWORD@postgres:5432/tg_shop
# POSTGRES_PASSWORD задаётся в .env (без префикса DATABASE_)
POSTGRES_PASSWORD=changeme

ENCRYPTION_KEY=generate-with-python-c-from-cryptography.fernet-import-Fernet-print-Fernet.generate_key-decode
