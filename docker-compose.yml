services:
  bot:
    build: .
    container_name: tg_shop_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:////app/data/shop.db
    volumes:
      - bot_data:/app/data

volumes:
  bot_data:
